services:
  backend:
    build: .
    container_name: clarity-backend
    ports:
      - "8000:8000"
    env_file: .env
    restart: unless-stopped
    dns:
      - 8.8.8.8
      - 1.1.1.1
    networks:
      - clarity-network

networks:
  clarity-network:
    driver: bridge
